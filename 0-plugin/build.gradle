plugins {
    id 'java-library'
}

dependencies {
    api project(":1-adapters")
}

project(':0-plugin') {
    apply plugin: 'application'

    application {
        mainClass = 'de.dhbwka.navigation.Main'
    }

    run {
        workingDir = rootProject.projectDir
    }
}